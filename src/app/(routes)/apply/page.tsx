import React, { Suspense } from 'react'
import { Admission } from './_component/Admission'

const ApplyPage = () => {
  return (
    <Suspense>
      <Admission />
    </Suspense>
  )
}

export default ApplyPage
